parameters:
  rust_version: stable

jobs:
- job: ${{ parameters.name }}
  ${{ if eq(parameters.rust_version, 'stable') }}:
    displayName: cargo test
  ${{ if ne(parameters.rust_version, 'stable') }}:
    displayName: cargo +${{ parameters.rust_version }} test
  ${{ if parameters.cross }}:
    strategy:
      matrix:
        Linux:
          vmImage: ubuntu-16.04
        MacOS:
          vmImage: macOS-10.14
        Windows:
          vmImage: windows-2019
  ${{ if not(parameters.cross) }}:
    variables:
      vmImage: ubuntu-16.04
  pool:
    vmImage: $(vmImage)
  steps:
  - template: install-rust.yml
  - script: cargo test --all
    displayName: Run tests
